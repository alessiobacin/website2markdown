---
name: github/awesome-copilot@mcp-cli
description: Local mirror for the upstream MCP CLI skill referenced by the project rules.
source: github/awesome-copilot@mcp-cli
---

# MCP CLI

This template includes a local mirror for `github/awesome-copilot@mcp-cli` so every skill referenced by the rules is present in the repository.

Use this skill when working with MCP flows through CLI-oriented workflows.

Local note: replace or expand this mirror with the upstream skill content if you vendor the full skill implementation.